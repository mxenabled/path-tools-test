pluginManagement {
  repositories {
    mavenCentral()
    gradlePluginPortal()
    mavenLocal()
    maven { url "https://jitpack.io" }
  }
}

plugins {
  id "com.mooltiverse.oss.nyx" version "2.3.0"
}

rootProject.name = "path-tools-test"

nyx {
  preset = "simple"
}
